# Интеграционные Проекты

Три комплексных проекта, объединяющих концепции из разных глав книги.

## Обзор

Эти проекты предназначены для закрепления материала после прохождения основных глав. Каждый проект занимает 4-7 часов и объединяет несколько тем.

## Проект 1: RSS Reader

**Расположение:** `exercises/project_rss_reader/`
**Время:** 6-8 часов
**Сложность:** Средняя

### Описание
Консольный RSS-агрегатор с поддержкой подписок и offline-режима.

### Объединяет концепции из:
- **Глава 12**: Eio для асинхронного HTTP
- **Глава 14**: XML парсинг (ezxmlm)
- **Глава 15**: URL validation (Uri)
- **Глава 18**: CLI с Cmdliner

### Возможности
- Добавление и удаление RSS-фидов
- Парсинг RSS 2.0 / Atom
- Асинхронная загрузка фидов
- Локальное хранилище постов
- CLI команды: `add`, `list`, `posts`, `refresh`

### Технологии
- `eio` — асинхронное I/O
- `eio-ssl` — HTTPS поддержка
- `ezxmlm` — XML парсинг
- `uri` — URL validation
- `cmdliner` — CLI

### Запуск
```bash
cd exercises/project_rss_reader
dune build
dune exec rss-reader add https://example.com/feed.xml
dune exec rss-reader posts
```

---

## Проект 2: Schema Validator

**Расположение:** `exercises/project_schema_validator/`
**Время:** 4-6 часов
**Сложность:** Средняя

### Описание
Валидатор JSON-данных против упрощённых JSON-схем.

### Объединяет концепции из:
- **Глава 10**: Фантомные типы для type safety
- **Глава 14**: JSON processing (Yojson)
- **Глава 16**: Property-Based Testing (QCheck)

### Возможности
- Поддержка базовых типов: string, number, boolean, null, array, object
- Ограничения: minLength, maxLength, pattern, minimum, maximum, uniqueItems
- Комбинаторы: anyOf, allOf
- Информативные сообщения об ошибках с путями
- Property-based тесты

### Технологии
- `yojson` — JSON processing
- `str` — регулярные выражения
- `cmdliner` — CLI
- `qcheck` — property-based testing

### Запуск
```bash
cd exercises/project_schema_validator
dune build
dune exec schema-validator schema.json data.json
```

### Пример схемы
```json
{
  "type": "object",
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "age": { "type": "number", "minimum": 0 }
  },
  "required": ["name", "age"]
}
```

---

## Проект 3: Diff Generator

**Расположение:** `exercises/project_diff_generator/`
**Время:** 5-7 часов
**Сложность:** Высокая

### Описание
Инструмент для сравнения файлов и генерации unified diff (аналог `diff -u`).

### Объединяет концепции из:
- **Глава 08**: Алгоритмы (LCS — Longest Common Subsequence)
- **Глава 15**: Парсинг строк
- **Глава 18**: CLI приложения

### Возможности
- Алгоритм LCS с динамическим программированием
- Генерация unified diff формата
- Группировка изменений в hunks
- Настраиваемый контекст (default: 3 строки)
- Property-based тесты для корректности

### Технологии
- `cmdliner` — CLI
- `qcheck` — property-based testing

### Запуск
```bash
cd exercises/project_diff_generator
dune build
dune exec diff-gen file1.txt file2.txt
dune exec diff-gen file1.txt file2.txt -u 5  # 5 строк контекста
```

### Пример вывода
```diff
--- old.txt
+++ new.txt
@@ -1,3 +1,3 @@
 apple
-banana
+blueberry
 cherry
```

---

## Порядок выполнения

Рекомендуемый порядок (от простого к сложному):

1. **Schema Validator** — хорошее введение, не требует async
2. **RSS Reader** — знакомство с async I/O и парсингом
3. **Diff Generator** — наиболее сложный, требует понимания алгоритмов

Альтернативно, можно делать в порядке интереса к теме.

---

## Общие навыки

Все три проекта развивают:
- ✅ Проектирование с типами (type-driven design)
- ✅ Работа с Result типом для обработки ошибок
- ✅ Модульность и разделение ответственности
- ✅ CLI дизайн с Cmdliner
- ✅ Тестирование (unit + property-based)
- ✅ Работа с внешними библиотеками

---

## Расширения (опционально)

После завершения базовой версии, можно добавить:

### RSS Reader
- Поддержка OPML (импорт/экспорт подписок)
- Фильтрация по ключевым словам
- Экспорт в Markdown
- Web-интерфейс с Dream

### Schema Validator
- JSON Schema Draft 7 (oneOf, not, $ref)
- Форматы строк (email, uri, date-time)
- Фантомные типы для validated/unvalidated
- Кеширование compiled regex

### Diff Generator
- Цветной вывод (ANSI escape codes)
- Patience Diff алгоритм (как в Git)
- Side-by-side режим (-y флаг)
- Статистика (--stat флаг)
- Поддержка директорий

---

## Ресурсы

Каждый проект содержит:
- `GUIDE.md` — пошаговые инструкции
- `README.md` — обзор и примеры использования
- Stub-реализации с TODO комментариями
- Базовые тесты

## Критерии успеха

Проект считается завершённым, когда:
- [ ] Все функции реализованы (нет `failwith "todo"`)
- [ ] Все unit-тесты проходят (`dune runtest`)
- [ ] Property-based тесты проходят
- [ ] CLI работает с корректными и некорректными вводами
- [ ] Обработка ошибок покрывает edge cases
- [ ] Код хорошо типизирован и модульный

---

## Следующие шаги

После завершения проектов:
1. Изучите главы 19-20 (Advanced Topics)
2. Попробуйте создать свой собственный проект
3. Контрибьютьте в open-source OCaml проекты

Удачи! 🚀
