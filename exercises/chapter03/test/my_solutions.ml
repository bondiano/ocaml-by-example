(* Упражнения для главы 03: Быстрый обзор языка *)

(* Упражнение 1: Високосный год

   Год является високосным, если:
   - он делится на 4
   - НО не делится на 100
   - ИЛИ делится на 400

   Примеры:
   - 2000 — високосный (делится на 400)
   - 1900 — не високосный (делится на 100, но не на 400)
   - 2020 — високосный (делится на 4, но не на 100)
   - 2021 — не високосный
*)
let is_leap_year _year = failwith "todo"

(* Упражнение 2: Треугольник

   Определить тип треугольника по длинам сторон:
   - "equilateral" — все стороны равны
   - "isosceles" — две стороны равны
   - "scalene" — все стороны разные
   - "invalid" — не образует треугольник

   Условие существования треугольника: сумма любых двух сторон больше третьей
*)
let triangle _a _b _c = failwith "todo"

(* Упражнение 3: Raindrops (FizzBuzz вариант)

   Преобразовать число в строку по правилам:
   - Если делится на 3: добавить "Pling"
   - Если делится на 5: добавить "Plang"
   - Если делится на 7: добавить "Plong"
   - Если не делится ни на одно: вернуть строковое представление числа

   Примеры:
   - 28 = "Plong" (делится на 7)
   - 30 = "PlingPlang" (делится на 3 и 5)
   - 34 = "34" (не делится ни на что)
   - 105 = "PlingPlangPlong" (делится на 3, 5 и 7)
*)
let raindrops _n = failwith "todo"

(* Упражнение 4: Форматирование имени

   Создать функцию, которая принимает имя и фамилию и возвращает полное имя
   в формате "Фамилия, Имя" (с заглавной буквы).

   Примеры:
   - format_name "иван" "петров" -> "Петров, Иван"
   - format_name "АННА" "сидорова" -> "Сидорова, Анна"
*)
let format_name _first _last = failwith "todo"

(* Упражнение 5: Сумма списка

   Вычислить сумму элементов списка, используя pattern matching.
   НЕ используйте List.fold_left или другие функции высшего порядка.

   Примеры:
   - sum_list [1; 2; 3; 4] -> 10
   - sum_list [] -> 0
*)
let rec sum_list _lst =
  let _ = sum_list in (* silence unused-rec-flag warning *)
  failwith "todo"

(* Упражнение 6: N-е число Фибоначчи

   Вычислить N-е число Фибоначчи (0, 1, 1, 2, 3, 5, 8, ...).

   Примеры:
   - fib 0 -> 0
   - fib 1 -> 1
   - fib 6 -> 8
   - fib 10 -> 55
*)
let rec fib _n =
  let _ = fib in (* silence unused-rec-flag warning *)
  failwith "todo"
