# Руководство: Собираем OBE CLI

**Глава 2: Окружение и инструменты**
**Время: 2-3 часа**
**Сложность: Лёгкая**

## Введение

В этом упражнении вы соберёте **первый настоящий проект** — утилиту командной строки `obe`,
которую будете использовать на протяжении всей книги для проверки решений.

Вы изучите:
- Структуру OCaml-проекта с Dune
- Работу с библиотекой Cmdliner (CLI)
- Чтение файлов конфигурации (TOML)
- Сохранение данных в JSON
- Регулярные выражения в OCaml
- Модульную архитектуру

## Этап 0: Подготовка

### Структура проекта

```
obe/
├── bin/
│   ├── dune
│   └── main.ml           # CLI (Cmdliner) — ВАМ ЗАПОЛНИТЬ
├── lib/
│   ├── dune
│   ├── config.ml         # ВАМ ЗАПОЛНИТЬ
│   ├── progress.ml       # ВАМ ЗАПОЛНИТЬ
│   ├── next_exercise.ml  # ВАМ ЗАПОЛНИТЬ
│   ├── checker.ml        # ВАМ ЗАПОЛНИТЬ
│   ├── calendar.ml       # ВАМ ЗАПОЛНИТЬ
│   ├── scanner.ml        # ГОТОВО
│   ├── parser.ml         # ГОТОВО
│   ├── formatter.ml      # ГОТОВО
│   └── stats.ml          # ГОТОВО
├── dune-project
├── obe.opam
├── README.md
└── GUIDE.md              # этот файл
```

**Готовые модули** (`scanner.ml`, `parser.ml`, `formatter.ml`, `stats.ml`):
- Сканирование директорий
- Анализ кода с регулярными выражениями
- Форматирование таблиц
- Подсчёт статистики

**Модули с TODO** (ваша задача):
- `config.ml` — чтение `.ocaml-by-example.toml`
- `progress.ml` — сохранение/загрузка прогресса в JSON
- `next_exercise.ml` — умная логика выбора следующего упражнения
- `checker.ml` — запуск `dune test` и обработка результатов
- `calendar.ml` — визуализация календаря в стиле GitHub
- `main.ml` — сборка всего в CLI

### Зависимости

Убедитесь что установлены:

```bash
opam install cmdliner yojson
```

### Конфигурация

Файл `.ocaml-by-example.toml` уже создан в корне проекта.
Изучите его структуру:

```toml
[exercises]
order = ["chapter02", "chapter04", ...]

[exercises.chapter02]
name = "Окружение и инструменты"
difficulty = "easy"
total_exercises = 9

[progress]
file = ".obe-progress.json"

[goals]
daily_streak_target = 7
total_target = 120
```

---

## Этап 1: Config — Чтение конфигурации (30 мин)

**Файл**: `lib/config.ml`

### Задача

Реализовать:
1. `find_project_root () : (string, string) result`
   - Найти корень проекта (директорию с `.ocaml-by-example.toml`)
   - Начать с `Sys.getcwd ()`, идти вверх через `Filename.dirname`
2. `load_config (root : string) : (config, string) result`
   - Прочитать и распарсить TOML

### Подсказки

```ocaml
(* Проверка существования файла *)
Sys.file_exists "путь"

(* Конкатенация путей *)
Filename.concat dir ".ocaml-by-example.toml"

(* Родительская директория *)
Filename.dirname "/a/b/c"  (* => "/a/b" *)

(* Парсинг TOML — используйте библиотеку toml *)
(* Установка: opam install toml *)
(* Документация: https://github.com/ocaml-toml/To.ml *)
```

### Тесты

```bash
dune build
dune exec obe -- --help  # должен работать
```

---

## Этап 2: Progress — Сохранение прогресса (40 мин)

**Файл**: `lib/progress.ml`

### Задача

Реализовать:
1. `load (file : string) : progress`
   - Загрузить прогресс из JSON (если файл есть)
   - Вернуть пустой прогресс если файла нет
2. `save (file : string) (progress : progress) : unit`
   - Сохранить прогресс в JSON
3. `add_mark (progress : progress) (mark : mark) : progress`
   - Добавить отметку о прохождении упражнения
4. `calculate_streak (marks : mark list) : int * int`
   - Подсчитать текущую и максимальную серию

### Подсказки

```ocaml
(* Работа с JSON через Yojson *)
open Yojson.Basic

(* Парсинг из файла *)
let json = from_file "путь.json"

(* Запись в файл *)
to_file "путь.json" json

(* Создание JSON *)
let json = `Assoc [
  ("marks", `List [...]);
  ("current_streak", `Int 3);
]

(* Извлечение значений *)
let open Yojson.Basic.Util in
let marks = json |> member "marks" |> to_list
```

### Формат JSON

```json
{
  "marks": [
    {
      "date": "2026-02-21",
      "chapter": "chapter02",
      "exercise_num": 1,
      "status": "Pass"
    }
  ],
  "current_streak": 3,
  "longest_streak": 5,
  "total_exercises_done": 12
}
```

### Тесты

```bash
# Создайте тестовый JSON файл
echo '{"marks":[],"current_streak":0,"longest_streak":0,"total_exercises_done":0}' > test-progress.json

# Протестируйте загрузку в utop
utop
# open Obe_lib.Progress;;
# let p = load "test-progress.json";;
```

---

## Этап 3: Next_exercise — Умная логика (40 мин)

**Файл**: `lib/next_exercise.ml`

### Задача

Реализовать:
1. `current_chapter () : (string, string) result`
   - Получить имя текущей главы из `Sys.getcwd ()`
   - Извлечь `Filename.basename`
   - Проверить что это `chapter*` или `appendix_*`
2. `find_next (config : config) (progress : progress) : (next_info, string) result`
   - Итерироваться по `exercises_order`
   - Найти первое упражнение которое не `Pass` и не `Skipped`
3. `is_chapter_complete (chapter : string) (progress : progress) (config : config) : bool`
   - Проверить что все упражнения главы завершены

### Подсказки

```ocaml
(* Проверка префикса строки *)
String.starts_with ~prefix:"chapter" "chapter02"  (* => true *)

(* Получение текущей директории *)
let cwd = Sys.getcwd ()

(* Извлечение имени директории *)
let basename = Filename.basename "/a/b/chapter02"  (* => "chapter02" *)
```

### Тесты

```bash
cd exercises/chapter02
dune exec obe -- check  # должно определить chapter02
```

---

## Этап 4: Checker — Запуск тестов (30 мин)

**Файл**: `lib/checker.ml`

### Задача

Реализовать:
1. `run_tests (chapter : string) : test_result`
   - Запустить `dune test` в директории главы
   - Собрать вывод и определить Pass/Fail
2. `mark_exercise (config : config) (progress : progress) (chapter : string) (num : int) (status : mark_status) : progress`
   - Добавить отметку о прохождении
3. `motivation_message (streak : int) : string`
   - Вернуть мотивационное сообщение в зависимости от streak

### Подсказки

```ocaml
(* Запуск внешней команды *)
let cmd = Printf.sprintf "cd %s && dune test 2>&1" dir in
let ic = Unix.open_process_in cmd in
let output = In_channel.input_all ic in
let status = Unix.close_process_in ic in
...

(* Проверка успеха *)
match status with
| Unix.WEXITED 0 -> Pass
| _ -> Fail
```

### Мотивационные сообщения

```ocaml
match streak with
| 1 -> "🌟 Отличное начало!"
| 3 -> "🔥 Три дня подряд! Так держать!"
| 7 -> "🏆 Неделя подряд! Невероятно!"
| n when n >= 10 -> Printf.sprintf "👑 %d дней! Вы легенда!" n
| _ -> "✅ Отлично!"
```

---

## Этап 5: Calendar — Визуализация (30 мин)

**Файл**: `lib/calendar.ml`

### Задача

Реализовать:
1. `format_calendar (marks : mark list) : string`
   - Создать календарь в стиле GitHub (7 недель = 49 дней)
   - Показать активность: `█` (есть отметка), `░` (нет)
2. `format_stats (progress : progress) : string`
   - Форматировать статистику: streak, longest streak, total

### Подсказки

```ocaml
(* Работа с датами *)
(* Простой подход: использовать строки "YYYY-MM-DD" *)
(* и преобразовывать в Unix timestamp для сравнения *)

(* Создание сетки 7x7 *)
let grid = Array.make_matrix 7 7 ' ' in
grid.(0).(0) <- '█';
...

(* Вывод календаря *)
Array.iter (fun row ->
  Printf.printf "Mo  ";
  Array.iter (fun cell -> Printf.printf "%c" cell) row;
  Printf.printf "\n"
) grid
```

### Пример вывода

```
    February 2026
Mo  ░██░░░█
Tu  █████░░
We  ░█████░
Th  ██░░███
Fr  ░░█████

🔥 Current streak: 5 days
🏆 Longest streak: 12 days
📊 Total: 23/120 exercises (19%)
```

---

## Этап 6: CLI — Сборка команд (40 мин)

**Файл**: `bin/main.ml`

### Задача

Подключить все модули и реализовать команды:
1. `obe check` — проверить текущее упражнение
2. `obe progress` — показать прогресс
3. `obe show` — календарь
4. `obe skip` — пропустить упражнение
5. `obe stats` — статистика по сложности
6. `obe reset` — сбросить решения
7. `obe test` — запустить тесты

### Подсказки

См. готовые заглушки в `bin/main.ml`.

Замените TODO на реальные вызовы модулей:

```ocaml
let check () =
  (* 1. Config.find_project_root *)
  (* 2. Config.load_config *)
  (* 3. Progress.load *)
  (* 4. Next_exercise.current_chapter *)
  (* 5. Checker.run_tests *)
  (* 6. Checker.mark_exercise (если Pass) *)
  (* 7. Progress.save *)
  (* 8. Вывести результат + мотивацию *)
  ...
```

### Тесты

```bash
dune build
dune exec obe -- check
dune exec obe -- progress
dune exec obe -- show
```

---

## Этап 7: Финальное тестирование (20 мин)

### Полный цикл

1. Соберите проект:
   ```bash
   dune build
   ```

2. Перейдите в любую главу:
   ```bash
   cd exercises/chapter04
   ```

3. Запустите проверку:
   ```bash
   dune exec obe -- check
   ```

4. Посмотрите прогресс:
   ```bash
   dune exec obe -- progress
   ```

5. Покажите календарь:
   ```bash
   dune exec obe -- show
   ```

6. Пропустите упражнение:
   ```bash
   dune exec obe -- skip
   ```

### Опциональная установка

Установите глобально:

```bash
dune install
```

Теперь можно использовать просто `obe`:

```bash
obe check
obe progress
obe show
```

---

## Поздравляем! 🎉

Вы собрали первый настоящий OCaml-проект!

Теперь у вас есть инструмент который будет:
- Проверять ваши решения
- Отслеживать прогресс
- Мотивировать серией (streak)
- Показывать статистику

Используйте `obe` на протяжении всей книги для проверки упражнений.

---

## Дополнительные задания (опционально)

### 1. Цветной вывод

Добавьте ANSI цвета:
- ✅ зелёный для Pass
- ❌ красный для Fail
- 🔥 оранжевый для streak

```ocaml
let green s = "\027[32m" ^ s ^ "\027[0m"
let red s = "\027[31m" ^ s ^ "\027[0m"
```

### 2. Интерактивный режим

Добавьте команду `obe interactive`:
- Показывать список упражнений
- Позволять выбрать следующее
- Автоматически запускать `obe check`

### 3. Экспорт статистики

Добавьте команду `obe export`:
- Экспорт прогресса в CSV
- Экспорт календаря в SVG (как GitHub)

### 4. Уведомления

Интеграция с системными уведомлениями:
- При прохождении упражнения
- При достижении streak milestone

---

## Ресурсы

- [Cmdliner Tutorial](https://erratique.ch/software/cmdliner/doc/tutorial.html)
- [Yojson Documentation](https://ocaml-community.github.io/yojson/)
- [OCaml Str Module](https://ocaml.org/api/Str.html)
- [Dune Documentation](https://dune.readthedocs.io/)
